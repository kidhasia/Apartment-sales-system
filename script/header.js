const menuIcon = document.getElementById('hambeger-menu');
const headerNavCollection =document.querySelector('.header-content');

function clicked(){
    headerNavCollection.classList.toggle('active-nav');
}